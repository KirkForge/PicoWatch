include LICENSE
include README.md
include CHANGELOG.md
include STATE.md
# Rule YAML files are now bundled inside src/picowatch/rules/ as package data.
# The top-level rules/ directory is kept for Docker volume mounts and dev use only.
# It is NOT included in sdist to avoid duplicate corpus drift.
